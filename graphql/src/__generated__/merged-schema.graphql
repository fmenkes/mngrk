type Author {
  name: String!
}

type Book {
  id: ID!
  title: String!
  genre: String
  isbn: String!
  pages: Int
  author: Author
}

type Query {
  author(id: ID!): Author
  authors: [Author]
  book(id: ID!): Book
  books: [Book]
}
